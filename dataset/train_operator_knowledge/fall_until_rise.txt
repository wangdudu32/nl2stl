operator: fall_until_rise
meaning: "fall(φ until rise(ψ)) 表示以 rise 事件为终点的 until 关系整体从 true 转为 false。"
cases:
  - semantic: "无界 until_rise 条件被作为整体观察到失效，终点本身是一个 rise 转移事件。"
    cue: "the condition that x holds until y enters a closed region at a future time changes from true to false：外层 fall 作用在完整 until_rise 关系上。"
    pattern: "fall(φ until rise(ψ))"

  - semantic: "有界 until_rise 条件被作为整体观察到失效，终点是在指定未来窗口内出现的 rise 事件。"
    cue: "the condition that a state keeps holding until another signal shifts to a target value within the future window is detected to change from true to false：fall 作用在有界 until_rise 整体上。"
    pattern: "fall(φ until [a:b] rise(ψ))"

  - semantic: "有界 until_rise 条件整体失效，且 until 的终点是信号进入指定区间的 rise 事件。"
    cue: "the condition changes from true to false: x keeps holding until y enters a range within the future window：外层 fall 捕捉整段 until_rise 关系失效，右端 rise 保留进入区间语义。"
    pattern: "fall(φ until [a:b] rise(lower < y < upper))"

  - semantic: "无界 until_rise 条件整体失效，且右端终点是信号进入半开区间的 rise 事件。"
    cue: "the condition that x keeps within its range until y enters a half-open range in the future shifts from true to false：fall 作用在完整无界 until_rise 关系上。"
    pattern: "fall(φ until rise(lower < y <= upper))"
