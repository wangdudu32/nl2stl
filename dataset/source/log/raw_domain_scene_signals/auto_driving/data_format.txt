{
    id:"第i条数据(00000,00001,00002,...)",
    stl_template:"stl公式模板",
    domain:"这条数据所属领域",
    scene:"这条数据所属场景",
    trace_info:"填充模板的 时间&谓词 追踪信息",
    stl:"完整版本的stl公式",
    signals_info:[对每个信号信息简要介绍(包含单位信息)],
    time_info:[对填充的时间单位信息进行简要说明],
    nl_Literal_translation_en:"直接根据stl公式直译成对应的stl公式【公式翻译腔】【英文】【需要确保与stl公式语义一致性】",
    nl_Literal_translation_zh:"直接根据stl公式直译成对应的stl公式【公式翻译腔】【中文】【需要确保与stl公式语义一致性】",
    nl_Paraphrase_en:"对nl_Literal_translation采用工程化的常用表述进行意译，【英文】【需要保证与直译的语义一致性】",
    nl_Paraphrase_zh:"对nl_Literal_translation采用工程化的常用表述进行意译，【中文】【需要保证与直译的语义一致性】",
}

补充信息：
stl公式:
G((rise(front_distance < 12) ∧ ego_speed > 60) 
  -> 
  (F[0,2](brake_cmd = 1) ∧ F[0,4](ego_speed < 35) ∧ G[0,6](lane_offset <= 0.3)))
  
公式翻译腔：
对所有时刻，如果 front_distance < 12 出现上升沿，并且 ego_speed > 60，则在接下来的 0 到 2 s 内应满足 brake_cmd = 1，在接下来的 0 到 4 s 内应满足 ego_speed < 35，并且在接下来的 0 到 6 s 内应始终满足 lane_offset <= 0.3。
追踪信息:
G          → 对所有时刻
rise(...)  → 出现上升沿
F[0,2]     → 在接下来的 0 到 2 s 内
F[0,4]     → 在接下来的 0 到 4 s 内
G[0,6]     → 在接下来的 0 到 6 s 内始终

需求规约风格(意译)：
当高速跟车过程中前方车辆距离首次低于 12 m 时，自车控制系统应在 2 s 内发出制动指令，在 4 s 内将车速降低至 35 km/h 以下，并在触发后的 6 s 内将横向车道偏移控制在 0.3 m 以内。
追踪信息:
rise(front_distance < 12) ∧ ego_speed > 60 → 当高速跟车过程中前方车辆距离首次低于 12 m 时
F[0,2](brake_cmd = 1) → 在 2 s 内发出制动指令
F[0,4](ego_speed < 35) → 在 4 s 内将车速降低至 35 km/h 以下
G[0,6](lane_offset <= 0.3) → 在触发后的 6 s 内将横向车道偏移控制在 0.3 m 以内

